# 视图过渡

视图过渡提供页面与页面之间的过渡效果。此功能依赖浏览器的视图过渡支持，该支持目前是实验性的，可用范围有限。

视图过渡开箱即用。它只会在支持的浏览器中生效，避免在一些过时的浏览器中出现意外效果。

<br />

<video autoPlay loop muted>
  <source src="/videos/view-transition.mp4" />
</video>

不过，你也可以通过以下配置关闭它：

```yml filename="_config.cupertino.yml"
view_transition: false
```
